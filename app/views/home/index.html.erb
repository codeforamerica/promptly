    <div class="container"> <!-- main container -->
    	<h1>Landshark</h1>
        <div class="well">
            <%= render 'add_user' %>
    	</div>
        <div class="row-fluid">
            <div class="span6">
                <%= render 'upcoming', :action => 'list_upcoming' %>
            </div>
            <div class="span6">
            	<%= render 'recent',  :action => 'list_recent' %>
            </div>
        </div>

</div>
