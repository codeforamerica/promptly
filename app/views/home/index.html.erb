    <div class="container"> <!-- main container -->
    	<h1>Landshark</h1>
        <div class="well">
            <%= render 'add_user' %>
    	</div>
        <div class="row-fluid">
            <div class="span6">
             <%= render(:partial => 'upcoming', :collection => @upcoming, :as => :upcoming) || 'There are no upcoming messages' %>
            </div>
            <div class="span6">
             <%= render(:partial => 'recent', :collection => @recents, :as => :recents) || 'There are no recent messages' %>
            </div>
        </div>

</div>
