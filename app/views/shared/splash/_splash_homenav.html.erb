<div class="navbar transparent"> <!-- navbar -->
	<div class="navbar-inner">
		<div class="container">
			<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar icon-white"></span>
				<span class="icon-bar icon-white"></span>
				<span class="icon-bar icon-white"></span>
			</button>
			
				<% if user_signed_in? %>
					<span><%= link_to "Admin", admin_organization_dashboard_path(@organization.id), class: 'btn btn-success' %></span>					
					<div class="login pull-right">
					  <span><%= link_to "Welcome, #{current_user.name}!", root_path %> | <%= link_to "Sign out", destroy_user_session_path, method: :delete %></span>
					</div>
        <% else %>
        		<div class="login pull-right">
            <span>Already have an account? <%= link_to "Sign in", new_user_session_path, :class => 'signin' %></span>
          	</div>
        <% end %>
		</div>
	</div>
</div> <!-- /navbar -->
