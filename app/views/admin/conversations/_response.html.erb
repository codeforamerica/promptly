<% if @responses != [] %>
<table class="small-12 twelve columns">
  <thead>
    <th>Message content</th>
    <th>From phone</th>
    <th>Sent date</th>
  </thead>
  <tbody>
    <% @responses.each do |response| %>
    <tr>
      <td class="small-8"><%= response.message %></td>
      <td><%= number_to_phone(response.from_number, :area_code => true) %></td>
      <td><%= response.date.to_s(:date_format) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<% end %>