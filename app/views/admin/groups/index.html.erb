<div class="row-fluid">
  <span class="pageTitle span3">All Groups</span>
  <div class="span11">
    <table class="table table-striped">
    	<thead>
    		<th>Group name</th>
    		<th>Group description</th>
    	</thead>
      <tbody>
      <% @groups.each do |group| %>
        <tr>
  	      <td><%= link_to_if(can?(:read, Group), group.name, admin_organization_group_path(@organization.id ,group.id)) {} %></td>
  			  <td><%= group.description %></td>
          <% if can? :edit, Group %>
    			  <td>
              <%= link_to "Edit", edit_admin_organization_group_path(@organization.id, group.id), :class => 'btn btn-default' %>
              &nbsp;
              <%= link_to "Delete", [:admin, @organization, group], :confirm => "Are you sure?", :method => :delete, :class => 'btn btn-danger' %>
            </td>
          <% end %>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="row-fluid">
  <% if can? :create, Group %>
    <div class="pageHeader span12">
      <%= link_to 'New group', new_admin_organization_group_path(@organization.id), :class => 'cta-button btn btn-primary' %>
    </div>
  <% end %>
</div>
