<p><%= f.label :name %></p>
<p><%= f.text_field :name %></p>
 
<p><%= f.label :email %></p>
<p><%= f.text_field :email %></p>
 
<% if can?(:edit, User) %>
  <div class="field">
    <div class="control-group">
      <%= f.label :roles, 'Select a role' %>
      <div class="controls">
        <%= f.collection_select :roles, User.valid_roles, :to_s, :to_s %>
      </div>
    </div>
  </div>
  <% end %>